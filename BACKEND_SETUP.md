# Настройка бекенда для сбора email подписок на Vercel

## 🚀 Быстрая настройка (Рекомендуется - через Telegram)

### Шаг 1: Создайте Telegram бот
1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и назовите бота (например, "DreamsTalk Subscriptions Bot")
4. Скопируйте **токен бота** (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получите ваш Chat ID
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Нажмите "Start"
3. Бот отправит вам ваш **Chat ID** (число, например `123456789`)

### Шаг 3: Деплой на Vercel
1. Установите Vercel CLI (если ещё не установлен):
   ```bash
   npm install -g vercel
   ```

2. В корне проекта выполните:
   ```bash
   vercel
   ```

3. Следуйте инструкциям:
   - Выберите scope (ваш аккаунт)
   - Link to existing project? → No
   - Project name → dreamsite (или любое другое)
   - Directory → ./
   - Override settings? → No

### Шаг 4: Добавьте переменные окружения
После деплоя, добавьте секреты в Vercel:

```bash
vercel env add TELEGRAM_BOT_TOKEN
# Вставьте токен бота, который получили от BotFather

vercel env add TELEGRAM_CHAT_ID
# Вставьте ваш Chat ID, который получили от @userinfobot
```

Выберите для обеих переменных:
- Environment: `Production`, `Preview`, `Development` (все три)

### Шаг 5: Передеплойте проект
```bash
vercel --prod
```

### Шаг 6: Обновите frontend код
Откройте `script.js` и раскомментируйте блок отправки на backend:

```javascript
// Было закомментировано:
// await fetch('/api/subscribe', {
//     method: 'POST',
//     headers: { 'Content-Type': 'application/json' },
//     body: JSON.stringify({ email })
// });

// Раскомментируйте и измените на:
const response = await fetch('/api/subscribe', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email })
});

if (!response.ok) {
    throw new Error('Failed to subscribe');
}
```

---

## 📧 Альтернатива: Email уведомления через SendGrid (бесплатно до 100 писем/день)

### Шаг 1: Создайте аккаунт SendGrid
1. Перейдите на [sendgrid.com](https://sendgrid.com)
2. Зарегистрируйтесь (бесплатный план - 100 писем/день)
3. Создайте API Key в настройках

### Шаг 2: Установите зависимости
```bash
npm install @sendgrid/mail
```

### Шаг 3: Добавьте переменные окружения
```bash
vercel env add SENDGRID_API_KEY
# Вставьте API ключ из SendGrid

vercel env add NOTIFICATION_EMAIL
# Ваш email, на который будут приходить уведомления
```

### Шаг 4: Раскомментируйте код SendGrid в `api/subscribe.js`
Найдите в файле `api/subscribe.js` секцию "ВАРИАНТ 2" и раскомментируйте её.

---

## 🔍 Просмотр логов и данных

### Логи Vercel
Посмотреть все подписки можно в логах Vercel:
1. Откройте [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите проект
3. Перейдите в "Logs"
4. Все подписки логируются с полной информацией

### Telegram уведомления
Каждая новая подписка автоматически отправится вам в Telegram с информацией:
- 📧 Email адрес
- 🕐 Время подписки
- 🌐 IP адрес
- 📱 User Agent (браузер/устройство)

---

## 🗄️ Дополнительно: Сохранение в Google Sheets

Если хотите автоматически добавлять emails в Google Таблицу:

1. Установите googleapis:
   ```bash
   npm install googleapis
   ```

2. Следуйте [этой инструкции](https://developers.google.com/sheets/api/quickstart/nodejs) для настройки Google Sheets API

3. Добавьте код в `api/subscribe.js`

---

## ✅ Проверка работы

После настройки проверьте работу:

1. Откройте ваш сайт
2. Введите тестовый email в форму подписки
3. Проверьте:
   - ✅ Telegram бот прислал уведомление
   - ✅ В логах Vercel появилась запись
   - ✅ На сайте показалось сообщение об успехе

---

## 🆘 Проблемы?

Если что-то не работает:

1. Проверьте логи: `vercel logs`
2. Убедитесь, что переменные окружения добавлены правильно
3. Проверьте, что бот создан корректно (отправьте ему `/start`)
4. Проверьте консоль браузера на ошибки (F12 → Console)

---

## 💡 Рекомендация

**Лучший вариант:** Telegram бот
- ✅ Бесплатно
- ✅ Мгновенные уведомления
- ✅ Не требует дополнительных сервисов
- ✅ Можно отправлять в группу/канал
- ✅ История всех подписок в чате
