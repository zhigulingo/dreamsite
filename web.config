<?xml version="1.0"?>
<configuration>
    <system.webServer>
        <rewrite>
            <rules>
                <!-- Send crawler requests with `_escaped_fragment_` to prerendered snippets. -->
                <rule name="rewrite crawler root to snippet" stopProcessing="true">
                    <match url="^/?$" ignoreCase="false" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{QUERY_STRING}" pattern="(^|&)_escaped_fragment_=" ignoreCase="false" />
                        <add input="{URL}" pattern="^(dist|img|snippets)" ignoreCase="false" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="snippets/1.html" />
                </rule>
                <rule name="rewrite crawler pages to snippets" stopProcessing="true">
                    <match url="^([.a-zA-Z0-9-_]+(?:/[.a-zA-Z0-9-_]+)*)/?$" ignoreCase="false" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{QUERY_STRING}" pattern="(^|&)_escaped_fragment_=" ignoreCase="false" />
                        <add input="{URL}" pattern="^(dist|img|snippets)" ignoreCase="false" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="snippets/{R:1}.html" />
                </rule>
                <!-- SPA routing for regular visitors. -->
                <rule name="rewrite home to index.html" stopProcessing="true">
                    <match url="^/?$" ignoreCase="false" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{URL}" pattern="^(dist|img|snippets)" ignoreCase="false" negate="true" />
                        <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="index.html" appendQueryString="true" />
                </rule>
                <rule name="rewrite assets under pages" stopProcessing="true">
                    <match url="^.+(dist|img|snippets)(/(.*))?" ignoreCase="false" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{URL}" pattern="^(dist|img|snippets)" ignoreCase="false" negate="true" />
                        <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="{R:1}{R:2}" appendQueryString="true" />
                </rule>
                <rule name="rewrite pages to index.html" stopProcessing="true">
                    <match url="^[.a-zA-Z0-9-_]+(/(.*))?" ignoreCase="false" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{URL}" pattern="^(dist|img|snippets)" ignoreCase="false" negate="true" />
                        <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="index.html" appendQueryString="true" />
                </rule>
            </rules>
        </rewrite>
    </system.webServer>
</configuration>
